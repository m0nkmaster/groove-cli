# Features Ideas yet to create

1.  Duplicate track
2.  Color
3.  Simpler naming - double quotes not required, file extension not required, assumes correct folders
4.  Chaining syntax - track(1).color(blue).sample(kick).delay(3).solo(1).play
5.  Variations to tracks
6.  Better UI/UX for error messages
7.  Chaining commands
8.  Autocomplete for sample selection
9.  Balance controls
10. Lots of 'effects/modifiers' per track (delay, reverb, flange...)
11. Envelopes
12. LFOs
13. Whole song effects
14. AI - Of course! Use to create patterns
15. Performance commands (quick variation switch, hi-pass, stutter)
16. Change at end of 'bar'
17. View tracks playing (expand list?) Only list specific tracks `list 1 2 3` for example or `list(1,2,3)` in chaining syntax
18. Basic logic (only in chaining) - e.g. if(1, playhead = 1).pattern(1, "x...").then.pattern(1, "xxxx").then(stop)